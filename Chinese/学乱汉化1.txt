do
    local Translations = {
        -- ================= 侧边栏菜单 (Sidebar Navigation) =================
        ["Home"] = "主页",
        ["Combat"] = "战斗",
        ["Auto Parry"] = "自动招架",
        ["Targeting"] = "目标锁定",
        ["Rescue"] = "救援",
        ["SEALED"] = "已封印",

        -- ================= 主页 (Home Tab / Player & Server) =================
        ["PLAYER & SERVER"] = "玩家与服务器",
        ["Player"] = "玩家",
        ["Game"] = "游戏",
        ["Server"] = "服务器",
        ["Script version"] = "脚本版本",
        ["Run status"] = "运行状态",
        ["Access"] = "权限等级",
        ["Premium"] = "高级版",
        ["Gakran engine ready"] = "Gakran 引擎已就绪",
        ["made with ❤️"] = "精心制作 ❤️",
        ["TAP THE CREST"] = "点击徽章",

        -- ================= 战斗功能 (Combat Tab) =================
        ["HEAVY TIMER"] = "重击计时器",
        ["Heavy cooldown"] = "重击冷却",
        ["Ready (7.0s cooldown)"] = "就绪 (7.0秒冷却)",
        ["Heavy timer on screen"] = "屏幕显示重击计时",
        ["Height above the bottom"] = "距离底部高度",
        ["ATTACK AUTOMATION"] = "攻击自动化",
        ["Auto attack"] = "自动攻击",
        ["Auto heavy attack"] = "自动重击",
        ["Auto block"] = "自动格挡",
        ["Auto dodge"] = "自动闪避",
        ["Attack mode"] = "攻击模式",
        ["Facing"] = "朝向目标",
        ["Anywhere"] = "任意方向",
        ["Camera"] = "视角方向",
        ["Attack arc"] = "攻击弧度",
        ["Attack range"] = "攻击距离",
        ["M1 interval"] = "轻击间隔",
        ["Extra heavy delay"] = "额外重击延迟",
        ["Dodge interval"] = "闪避间隔",
        ["COMBAT SETUP"] = "战斗设置",
        ["Smart auto equip"] = "智能自动装备",
        ["Equip combat style"] = "装备战斗风格",
        ["Stop combat inputs"] = "停止战斗输入",
        ["RUN"] = "运行",

        -- ================= 自动招架 (Auto Parry Tab) =================
        ["PARRY ENGINE"] = "招架引擎",
        ["Perfect auto parry"] = "完美自动招架",
        ["Parry heavy attacks"] = "招架重击",
        ["Style calibration"] = "流派校准",
        ["Adaptive"] = "自适应",
        ["Striker"] = "打击者",
        ["Parry range"] = "招架范围",
        ["Timing lead"] = "时机预判",
        ["Block hold"] = "格挡保持时长",
        ["Session results"] = "本次数据统计",
        ["perfect"] = "完美",
        ["hit"] = "击中",
        ["ADAPTIVE SAFEGUARDS"] = "自适应防护",
        ["Auto punish"] = "自动反击/惩罚",
        ["Angle-based facing check"] = "基于角度的朝向检查",
        ["Network compensation"] = "网络延迟补偿",
        ["Animation progress sync"] = "动画进度同步",
        ["Learn attack timing"] = "学习攻击时机",
        ["Feint protection"] = "假动作防范",
        ["Multi-attack queue"] = "多段攻击队列",
        ["Panic block"] = "紧急格挡",
        ["Panic health"] = "紧急格挡血量线",

        -- ================= 目标锁定 (Targeting Tab) =================
        ["PLAYER TARGET"] = "玩家目标设置",
        ["Target"] = "锁定目标方式",
        ["Nearest"] = "最近玩家",
        ["Refresh player list"] = "刷新玩家列表",
        ["Ignore friends"] = "忽略好友",
        ["Ignore same team"] = "忽略同队玩家",
        ["Aim lock"] = "自瞄锁定",
        ["Camera lock"] = "视角锁定",
        ["Aim smoothing"] = "瞄准平滑度",
        ["Spectate target"] = "观战目标",
        ["Teleport to target"] = "传送至目标",
        ["Teleport offset"] = "传送距离偏移",
        ["TARGET FILTERS"] = "目标过滤",
        ["Target field of view"] = "目标视角范围 (FOV)",
        ["Require line of sight"] = "需要视线无遮挡",
        ["Ignore downed targets"] = "忽略倒地目标",
        ["Minimum target health"] = "最低目标血量",
        ["Maximum target health"] = "最高目标血量",
        ["TARGET LOCK"] = "按键锁定设置",
        ["Lock keybind"] = "锁定快捷键",
        ["Toggle target lock"] = "切换目标锁定状态",
        ["Clear target lock"] = "清除锁定目标",
        ["Locked target"] = "当前锁定目标",
        ["Unlocked"] = "未锁定",
        ["TARGET WHITELIST"] = "目标白名单",
        ["Whitelist selected"] = "将选中玩家加入白名单",
        ["Remove selected"] = "从白名单移除选中玩家",
        ["Clear whitelist"] = "清空白名单",
        ["Whitelist"] = "白名单列表",

        -- ================= 救援与倒地 (Rescue Tab) =================
        ["DOWNED PLAYERS"] = "倒地玩家处理",
        ["Auto carry"] = "自动扛起/搬运",
        ["Auto grip"] = "自动处决/补人",
        ["Grip range extender"] = "处决距离延长",
        ["Carry assist key"] = "搬运辅助快捷键",
        ["Grip assist key"] = "处决辅助快捷键",
        ["Carry nearest"] = "搬运最近的倒地玩家",
        ["Grip nearest"] = "处决最近的倒地玩家",
        ["SELF RECOVERY"] = "自我复苏",
        ["Auto revive [experimental]"] = "自动复活 [实验性]",
        ["Experimental. The heart prompt wants eight beats, the game paces them at 0.72s and only turns the prompt clickable about 4.5s after you go down, so the prompt itself cannot be finished before an execution. This asks the respawn service directly instead. Eight hits while downed still trigger an execution, and in group fights that can land first."] = "实验性功能。心跳提示需要敲击8次，游戏节奏为0.72秒，且在你倒下约4.5秒后提示才可点击，因此提示本身无法在被处决前完成。此功能直接向重生服务发送请求。倒地时受到8次打击仍会触发处决，在混战中可能先生效。",

        -- ================= 状态栏与系统提示 (Status & System) =================
        ["Combat equip requested"] = "已请求装备战斗风格",
    }

    local function translateText(text)
        if not text or type(text) ~= "string" then return text end
        
        if Translations[text] then
            return Translations[text]
        end
        
        for en, cn in pairs(Translations) do
            if text:find(en) then
                return text:gsub(en, cn)
            end
        end
        
        return text
    end

    local function setupTranslationEngine()
        local success, err = pcall(function()
            local oldIndex = getrawmetatable(game).__newindex
            setreadonly(getrawmetatable(game), false)
            
            getrawmetatable(game).__newindex = newcclosure(function(t, k, v)
                if (t:IsA("TextLabel") or t:IsA("TextButton") or t:IsA("TextBox")) and k == "Text" then
                    v = translateText(tostring(v))
                end
                return oldIndex(t, k, v)
            end)
            
            setreadonly(getrawmetatable(game), true)
        end)
        
        if not success then
            warn("元表劫持失败:", err)
           
            local translated = {}
            local function scanAndTranslate()
                for _, gui in ipairs(game:GetService("CoreGui"):GetDescendants()) do
                    if (gui:IsA("TextLabel") or gui:IsA("TextButton") or gui:IsA("TextBox")) and not translated[gui] then
                        pcall(function()
                            local text = gui.Text
                            if text and text ~= "" then
                                local translatedText = translateText(text)
                                if translatedText ~= text then
                                    gui.Text = translatedText
                                    translated[gui] = true
                                end
                            end
                        end)
                    end
                end
                
                local player = game:GetService("Players").LocalPlayer
                if player and player:FindFirstChild("PlayerGui") then
                    for _, gui in ipairs(player.PlayerGui:GetDescendants()) do
                        if (gui:IsA("TextLabel") or gui:IsA("TextButton") or gui:IsA("TextBox")) and not translated[gui] then
                            pcall(function()
                                local text = gui.Text
                                if text and text ~= "" then
                                    local translatedText = translateText(text)
                                    if translatedText ~= text then
                                        gui.Text = translatedText
                                        translated[gui] = true
                                    end
                                end
                            end)
                        end
                    end
                end
            end
            
            local function setupDescendantListener(parent)
                parent.DescendantAdded:Connect(function(descendant)
                    if descendant:IsA("TextLabel") or descendant:IsA("TextButton") or descendant:IsA("TextBox") then
                        task.wait(0.1)
                        pcall(function()
                            local text = descendant.Text
                            if text and text ~= "" then
                                local translatedText = translateText(text)
                                if translatedText ~= text then
                                    descendant.Text = translatedText
                                end
                            end
                        end)
                    end
                end)
            end
            
            pcall(setupDescendantListener, game:GetService("CoreGui"))
            local player = game:GetService("Players").LocalPlayer
            if player and player:FindFirstChild("PlayerGui") then
                pcall(setupDescendantListener, player.PlayerGui)
            end
            
            while true do
                scanAndTranslate()
                task.wait(3)
            end
        end
    end

    task.wait(2)
    setupTranslationEngine()

    local success, err = pcall(function()
        loadstring(game:HttpGet("https://api.jnkie.com/api/v1/loaders/public/db030a8e7623efa9647d6a9864f76d6b3234d17500326b41c81a7245d35ab29e/download"))()
    end)

    if not success then
        warn("加载失败:", err)
    end
end

